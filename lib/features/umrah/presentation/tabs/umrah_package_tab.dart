import 'package:flutter/material.dart';

/// Predefined Umrah Packages Tab
class UmrahPackageTab extends StatelessWidget {
  const UmrahPackageTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Umrah Package List Placeholder',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
