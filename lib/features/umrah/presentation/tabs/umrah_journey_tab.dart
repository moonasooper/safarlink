import 'package:flutter/material.dart';

/// Basic Umrah Journey Calculator Tab
class UmrahJourneyTab extends StatelessWidget {
  const UmrahJourneyTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Umrah Journey Calculator Placeholder',
        style: TextStyle(fontSize: 16),
      ),
    );
  }
}
